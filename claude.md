# 藥品支付價提案產生器

單一 HTML 檔工具。匯入「藥品主檔」與「案件需求檔」兩份 Excel，產出：

- **提案 Word**（一份需求檔＝一案，多項藥品合併成一份 docx）
- **四分頁 Excel 報表**（藥價核定試算／分組統計／項目明細／項目明細含 0 元）
- **提案附件 Excel**（同分組＋ATC 前1/3/4/5碼＋ATC7碼，固定 6 分頁，A4 橫向）

全部運算在瀏覽器內完成，**不上傳資料、不需連外網路、無任何 `<script src>`**。

> 本文件是**當前規格的權威來源**。文末「變更歷程」僅供追溯，內容以本文前段為準。

---

## 1. 檔案

| 檔案 | 說明 |
|---|---|
| `dist/case_template.html` | **寄給同仁的單檔版**。一個檔、瀏覽器直接開，由 `build/bundle.mjs` 產生 |
| `index.html` | 主工具（分檔版，開發維護用）。`assets/` 需與它同層 |
| `assets/js/**` | 拆分後的模組，見 §7.2 |
| `legacy/case_template.html` | 重構前的單軌單檔版，凍結供比對 |
| `INTENT.md` | 雙軌分流架構重構規格 |
| `藥品主檔資料處理原則.md` | 法規與資料處理規則 [A1]~[D20]，**計算規則的權威來源** |
| `claude.md` | 本文件 |
| `修正說明_請更換檔案.md` | 給同仁的修正通知（舊版已發布後補發） |
| `資料邏輯說明.html` | 說明頁的單一 HTML 檔（與線上 Artifact 同內容，可離線開啟；字型改用系統預設） |
| `案件需求檔_範本.xlsx` | 由工具介面產生的空白範本（下拉選單＋填表說明分頁） |
| `範例輸出_提案.docx` | 假資料樣張，核對版面用 |
| `範例輸出_四分頁報表.xlsx` | 假資料樣張 |
| `範例輸出_提案附件.xlsx` | 假資料樣張，核對附件版面與列印範圍用 |
| `refactored_code_big5.txt` | 原 SAS 程式（Big5），規則來源 |
| `表格修改說明.pdf` | 核價試算表版面規格 |
| `專家提案附件.pdf` | **提案附件 Excel 的版面規格來源** |

**資料邏輯說明（給主管／同仁）**：<https://claude.ai/code/artifact/b7e3407f-2765-4a2e-bcfd-16995e155c3c>
（五個頁籤：總覽／共用規則／提案 Word／Excel 報表／注意事項。文中刻意不提 SAS，改稱「既有試算程式」。
2026-08 改版：主要機制改以 SVG 圖解呈現，統一「實線＝納入／虛線＝排除／藍＝本案／琥珀＝例外」的編碼，並支援深色主題。）

---

## 2. 輸入檔規格

### 2.1 案件需求檔（11 欄）

**一份檔＝一案。**每列一筆來文；**同一個 `CODE` 可出現多列**，讀檔時會合併成一項藥品。

| 欄位 | 層級 | 說明 |
|---|---|---|
| `報告人` | 案件 | 取第一個非空值 |
| `CODE` | 藥品 | 必填，對應主檔。**留空的列整列略過**；同一 `CODE` 可多列，合併為一項藥品 |
| `ATC_MODE` | 藥品 | 範本為**下拉選單**。`BOTH`＝兩者都做，表2 產生**兩張表**（**預設**；留空或無法辨識者一律視為 BOTH）；`FULL`＝ATC7 全等；`PREFIX5`＝前 5 碼相同 |
| `來文日期文號` | 藥品 | 每列填一筆。**一項藥品有 n 筆來文 → 同一 `CODE` 貼成 n 列，第 2 列起其他欄位全部留白**；唯一硬規定是每列都要有 `CODE`，沒有 `CODE` 的列整列略過（連同該列文號）。重複的（藥商＋文號）自動去重 |
| `藥品治療用途` | 藥品 | 留空則取主檔 `適應症` |
| `廠商建議價` | 藥品 | |
| `廠商成本` | 藥品 | 成本法輸入值 |
| `十國中位價` | 藥品 | **留空或 0 → 十國藥價法整欄印「無」** |
| `領有許可證` | 藥品 | 範本為**下拉選單**。`Y`／`N`，非 Y/N 時預設 `Y` |
| `十國加計比例` | 藥品 | 逗號分隔，例 `0,10` |
| `成本加計比例` | 藥品 | 逗號分隔，例 `15,20,25,30` |

> **不需輸入**：供應狀況、其他廠商回覆、專家意見。Word 保留編號清單＋項目文字＋底線，由承辦人自行填寫。
> `廠商占率` 已移除，改由系統計算。
>
> **同 `CODE` 多列的合併規則（`mergeCaseRowsByCode`）**
> - 依 `CODE` 首次出現順序分組，每組合併成**一列藥品**
> - 每個欄位取該組的**第一個非空值**（數值 0 視為有值，不會被後列覆蓋）
> - `來文日期文號`：全部保留，依出現順序去重
> - `ATC_MODE`：取聯集 —— 含 `BOTH`，或同時出現 `FULL` 與 `PREFIX5` → `BOTH`；否則取第一個非空值；全部留空 → `BOTH`
> - 結果：說明六核價表張數、表五列數 ＝ **相異 `CODE` 數**，不會因多列文號而重複
> 範本 `.xlsx` 附「填表說明」分頁，逐欄列出上述規則；`ATC_MODE`、`領有許可證` 以 Excel 資料驗證（下拉清單，套用至第 300 列）限制輸入。降級為 `.csv` 時無下拉與說明分頁。

### 2.2 藥品主檔

**必要欄位**
`CODE`、`藥品名稱`、`藥商名稱`、`分組代碼`、`分組名稱`、`ATC7碼`、
`PRICE{年月}`、`QTY{年}`、`AMT{年}`

**選用欄位**（缺漏時退回替代值或印底線）
`ATC名稱`、`分類`、`藥品分類`、`藥品分類_名稱`、`藥商簡稱`、`收載日期`、`生效日期`、
`成分名稱`、`核價成分名稱_對照檔`、`劑型`、`成分及含量`、`成分含量`、`適應症`、
`不良品暫停支付註記`、`不良品暫停支付前價格`、`藥價說明`

> **`QTY_ALL_{年}` 一律忽略**，所有表格只用 `AMT` / `QTY`。

**日期欄位容錯**：`生效日期`、`收載日期` 接受 西元 8 碼（`20240401`）／民國 7 碼（`1130401`）／Excel 日期序號。

---

## 3. 共用整備規則

讀檔後只跑一次，Word 與 Excel 共用同一份結果，兩邊數字必然一致。

### 3.1 年度視窗
支付價年月前 3 碼＝基準民國年。當期 `YR3` 預設為**基準年 − 1**；主檔若無該年度 `QTY`／`AMT` 欄，整個視窗自動往前推（最多回推 8 年）。
`YR1 = YR3-2`、`YR2 = YR3-1`。
> 例：`11504` → 基準年 115 → 近三年為民國 112、113、114。

### 3.2 0 元項目的年度歸零（[A2]）
依 `不良品暫停支付註記` 與 `PRICE{年月}` 分四種情形：

| 情形 | 條件 | 處理 |
|---|---|---|
| 1 | 註記 ＝ `Y` | 直接採計，**無年份邊界**（無論支付價為何） |
| 2 | 註記 ≠ `Y` 且 支付價 ≠ 0 | 直接採計 |
| 3 | 註記 ≠ `Y` 且 支付價 ＝ 0 | **晚於生效民國年**的年度其 `QTY`／`AMT` 視為 0；生效年當年及更早照原值保留 |
| 4 | 註記 ≠ `Y` 且 支付價為空值 | 直接採計（費用內含於其他項目，不另核價但仍有申報量） |

> 理由：項目可能 115 年某月才取消給付，114 年仍有真實申報資料。
> ⚠️ 第 21 次改版前漏了情形 1 的判斷，不良品暫停支付項目被誤歸零，連帶影響 [B6]／[B7]／[C9]。

### 3.3 分組層彙總（以 `分組代碼` 為單位）
- 三年 `AMT` / `QTY` 總計
- **支付價區間**：相異 `PRICE{年月}`（**含 0**）由小到大，Excel 用 ` / `、Word 用 `、`
- **項目數（[B5]）**：唯一不計入的情形是「註記 ≠ `Y` 且 `PRICE = 0`」。
  亦即 `PRICE ≠ 0` 計 1；`PRICE = 0` 但註記 ＝ `Y` 計 1；**`PRICE` 為空值也計 1**（費用內含於其他項目者）
- **分組項目總數**：該群全部列數（含 0 元）

> 法規「全民健康保險藥物給付項目及支付標準」已將「品項」全面改為「項目」，**所有輸出用語一律用「項目」**。

### 3.2.1 主檔排除條件 ⭐（在建模型之前就篩掉）

介面步驟 02 提供兩個核取方塊，**預設都勾選**：

| 條件 | 判定 | 理由 |
|---|---|---|
| 排除 `CODE` 前 3 碼為 `TPN` | `CODE.substring(0,3).toUpperCase() === 'TPN'` | 院內調製、個別醫院使用、已不再核定新代碼 |
| 排除 `不上網註記 = 'Y'` | 欄名依序找 `不上網註記`／`不上網`／`不上網註`／`不上網記號`，值為 `Y` | 不列於健保收載清單，非提案的比較對象 |

**篩選發生在 `buildGroupModel` 之前**，因此提案 Word、提案附件、四分頁報表看到的是**同一份母體**，
不會出現某一份含、另一份不含的情形。分組層的加總、占率分母、近三年平均申報量、級距判定
都會跟著改變 —— 這是刻意的，被排除的品項本來就不該進入比較基準。

> **本案需求檔內的 `CODE` 一律保留**，不受排除影響（避免本案藥品剛好符合排除條件而消失）。
> 介面診斷列會顯示排除筆數與實際採用筆數；相關函式：`excludeMaster()`／`isTpnCode()`／`offnetFlag()`。

### 3.3.1 兩條獨立規則 ⭐（Word 與 Excel 一致的根本）

整份工具只有這兩條規則，**兩者互不影響**，且所有輸出共用同一份結果：

| | 規則 | 決定什麼 | 實作 |
|---|---|---|---|
| **A. 採計** | 支付價 ＝ 0 且有 `生效日期` 時，**晚於生效民國年**的年度其 `QTY`／`AMT` 歸零；生效年當年及更早照原值保留 | 所有**金額與數量**的值，以及占率分母 | `amtAdj`／`qtyAdj`（§3.2） |
| **B. 列出／計數** | 支付價 ≠ 0；或 支付價 ＝ 0 且 `不良品暫停支付註記 = 'Y'` | 哪些**列**要出現、`項目數` 怎麼算 | `isCounted(r)`／`countValid()` |

**規則 B 不會影響規則 A 的加總範圍。** 所有「分組層的金額／數量」一律是**整個分組全部項目**（採規則 A 調整後）的加總，
包含依規則 B 不列出的項目。這正是 Word 表二「整組申報金額」與附件「分組小計」相等的原因。

**套用位置一覽**

| 輸出 | 列出哪些列（規則 B） | 分組層金額／數量（規則 A · 整組） |
|---|---|---|
| Word 表一 · 同分組 | ✅ 支付價 0 者以「－」顯示，表下加備註 | 占率分母＝整組 |
| Word 表二 · 同 ATC 碼 | ✅（`項目數`） | ✅ 整組申報金額／申報量 |
| Word 表五 | 每項藥品一列 | 廠商占率分母＝整組 |
| 提案附件 Excel | ✅ 支付價 0 者以「－」顯示 | ✅ 分組小計＝整組 |
| 四分頁 · 分頁2 分組統計 | ✅（`項目數`） | ✅ 分組總計 |
| 四分頁 · 分頁3 項目明細 | ✅ | 占率分母＝整組 |
| 四分頁 · 分頁4 明細(含0元) | ❌ 全部項目（查核用，刻意保留原始支付價 0） | 占率分母＝整組 |

> 對外文件（Word、提案附件）支付價 0 一律顯示「－」；分頁 4 是內部查核用，保留原值。

### 3.4 近三年平均申報量（[B6]）
分組收載年 ＝ 該分組所有項目 `收載日期` 的**最小值**（完整 12 碼分組代碼）。
三年視窗中**早於分組收載年的年度不納入**，分母固定為 3／2／1，**不因某年度全為 NULL 而縮減**。
> 例：分組於民國 113 年收載，視窗 112～114 → 112 年不計，平均 ＝（113 量 ＋ 114 量）÷ 2。
> ⚠️ 分組收載年**晚於**視窗最近一年（等同在當年度、也就是尚無完整 12 個月資料的那一年才收載）時，
> 近三年均無申報資料，平均申報量為 **0**（輸出顯示 0，不是「－」），仍須列出。

### 3.4.1 同成分同劑型收載年（[B8]）
與 [B6] **粒度不同、必須各自獨立計算，不可合併**：

- key ＝ 分組代碼**前 8 碼 ＋ 第 10、11 碼**（共 10 碼）。
  排除第 9 碼（成分組成序號）與第 12 碼（規格量流水號），以涵蓋同成分同劑型的所有含量規格。
- value ＝ 該 key 所有項目 `收載日期` 的最小年度。
- 基準年（藥商銷售資料採計期間截止年）依 `price{年月}` 的**月份**判定：≥ 4 月取該民國年；< 4 月取前一年。
- `基準年 − 同成分同劑型收載年 ≥ 15` → **第三 B 大類**（`row.cat3B` / `group.cat3B`）。

依據：112 年 3 月 23 日修正之全民健康保險藥品價格調整作業辦法第 21 條。

### 3.5 同分組每月申報金額
＝ **該分組當期年度 `AMT` 總計 ÷ 12**。分子是整個分組所有項目加總，不是本案單一項目。
**這是加成級距的唯一判定依據。**

---

## 4. 核價計算

### 4.1 加成級距（依法條「小於等於」）

| 同分組每月申報金額 | 十國藥價法 | 成本價法 |
|---|---|---|
| ≤ 500,000 | 加 20% | 加計 50% |
| > 500,000 且 ≤ 1,000,000 | 加 10% | 加計 40% |
| > 1,000,000 | 0% | 加計 30% |

同時決定 Word 註1／註2 引述的法條文字。

### 4.2 浮點誤差防護 ⭐（[C11]）

分兩條路徑，**不可混用**：

| 用途 | 函式 | 有效位數 |
|---|---|---|
| 價格截斷（[C12] 第 40 條、成本法步驟 2） | `truncatePrice()`／`truncate2()` | `toPrecision(10)`，依處理原則 |
| 分組層金額／數量總計、級距判定 | `fpClean()`／`floorAt()` | `toPrecision(15)` |

> 為何總計不能用 10 位：兆級申報金額的有效位數超過 10 位，`toPrecision(10)` 會把真實數值一起磨掉。
>
> ⚠️ 處理原則所附的參考碼只在**縮放前**清一次尾差，但 `Math.floor(v * 100)` 會再生一次
> （`4.56 × 100 = 455.99999999999994`），導致 `3.8 × 1.2` 被誤截為 `4.55`。
> 本實作在**縮放前後各清一次**（`floorScaled()`），結果為正解 `4.56`。


```js
/* 只清第 15 位有效位數以後的雜訊，真實資料不受影響 */
function fpClean(v){
  if (typeof v !== 'number' || !isFinite(v)) return v;
  return Number(v.toPrecision(15));
}
function floorAt(v, dec){
  var f = Math.pow(10, dec);
  return Math.floor(fpClean(v * f)) / f;
}
```

**套用位置**：`article40RoundDown()` 判斷分支前、`costRefPrice()` 的第 2 步、
`buildGroupModel()` 的 `amtSum`／`qtySum`／`avgQty3`／`monthlyAmt`、`amtTier()` 判定前。

> 為何是 `toPrecision(15)`：double 有效位數約 15–17 位，取 15 位可清表示誤差又不動真實資料。
> 驗證：`4.55999 → 4.55` ✓ 未被誤清、`500000.00000000006 → 500000` 清掉、`500000.0000001` 保留。

### 4.3 第 40 條 · 小數處理（無條件捨去）
```
v < 5   → floorAt(v, 2)
v < 50  → floorAt(v, 1)
否則    → Math.floor(v)          // v 已先 fpClean
```

### 4.4 十國藥價法
```
調高後支付價 = article40RoundDown( 十國中位價 × (1 + 加計比例) )
```

### 4.5 成本價法
```
step1  base = 成本 × (1 + 加計比例)
step2  base = floorAt(base, 2)                    // 取小數 2 位，無條件捨去
step3  if 領有許可證 == 'Y'  base = base × (1 + 0.05 + 0.0005)
step4  調高後支付價 = article40RoundDown(base)
```
> `0.05` ＝ 營業稅、`0.0005` ＝ 藥害救濟徵收金 0.05%。

### 4.6 整體藥費／財務衝擊
```
整體藥費 = 調高後支付價 × 同分組近三年平均申報量
財務衝擊 = (調高後支付價 − 現行健保價) × 同分組近三年平均申報量
```

### 4.7 金額顯示級距（[D19]）

改依處理原則實作兩支函式：

`chineseAmount2(v)` — 簡化顯示（`formatAmt()` 去掉尾字「元」後即為表格用）

| 範圍 | 顯示 | 進位 | 範例 |
|---|---|---|---|
| < 1萬 | 實數加千分位 | 整數 | `823元` |
| 1萬 ～ < 1,000萬 | 萬 | 小數 1 位 | `50,000` → `5.0萬` |
| 1,000萬 ～ < 1億 | 億 | 小數 2 位 | `10,000,000` → `0.10億` |
| 1億 ～ < 1,000億 | 億 | 小數 1 位 | `10,020,000,000` → `100.2億` |
| 1,000億 ～ < 1兆 | 兆 | 小數 2 位 | `999,999,999,999` → `1.00兆` |
| ≥ 1兆 | 兆 | 小數 1 位 | `1,500,000,000,000` → `1.5兆` |

`chineseAmount(v, decimals=3, unit='元')` — 完整展開式，供正式文件／精確金額使用
（`123,456,789` → `1億2,345萬6,789元`）。四分頁分頁 1 以 `chineseAmount(v, 0)` 呼叫。

> ⚠️ 尾 0 現在**會顯示**（`5.0萬` 而非 `5萬`），這是處理原則規定的固定小數位。

### 4.8 占率位數（`adaptivePct`）
`0 → '0%'`；`|v| ≥ 10 → 0 位`；`≥ 1 → 1 位`；`< 1 → 補到第一個有效數字`（例 `0.05%`）。

---

## 5. 提案 Word

### 5.1 基本格式

| 項目 | 設定 |
|---|---|
| 字型大小 | 14pt（`w:sz 28`），表格 10pt |
| 中文字型 | 標楷體（`w:eastAsia`） |
| 英文字型 | Times New Roman（`w:ascii`／`w:hAnsi`／`w:cs`） |
| 行距 | **固定行高 24pt**（`w:line="480" w:lineRule="exact"`） |
| 編號清單 | `word/numbering.xml` 多階：`一、` → `（一）` → `1.` → `(1)`，全部真編號 |
| 版面 | A4 直向，上下右 1134 / 左 1418 twips |
| 表格資料列 | **一律不加粗**；僅「（本案藥品）」四字加粗（8pt），標記於**項次下方** |
| 欄名年度 | 所有金額／數量欄名帶年度（`{YR3}年整組申報金額(元)`）；支付價欄帶年月（`現行健保支付價(115年8月)(元)`） |

**numbering 縮排**（14pt 中文字寬 280 twips 為單位；第一層起排 1 字元、第二層 1.5 字元）：
ilvl0 left 840 hang 560／ilvl1 left 1260 hang 840／ilvl2 left 1680 hang 420／ilvl3 left 2100 hang 420

> ⚠️ 不可在未使用 ilvl1 的情況下直接跳用 ilvl2，LibreOffice 會讓 ilvl1 計數器多跳一號。

### 5.2 版面

```
提案                                          報告人：{案件.報告人 → ＿＿＿＿＿＿}

案由：有關「{第一項藥品名稱}」建議調高{藥商名稱}藥品之健保支付價格案，提請討論。

說明：

一、依據{來文串接}辦理。
二、本案藥品健保收載情形說明如下：
    （一）{藥品名稱}（健保代碼 {CODE}）之主成分、劑型、含量為「{分組名稱}」，
         屬{藥品分類→用語}，屬健保收載{超過│未滿}15年之{分類→第◯}大類藥品，
         適應症「{適應症}」，ATC7碼為{ATC7碼} {ATC名稱}。
    （二）…（逐項一段，讓承辦人取捨）
    （三）查健保收載同分組藥品共計{x}項(P-)：          ［表1］
    （四）查健保收載相同ATC 7碼藥品共計{x}分組、{x}項目(P-)：  ［表2-A］
    （五）查健保收載相同ATC 5碼藥品共計{x}分組、{x}項目(P-)：  ［表2-B］
三、經洽詢廠商供應狀況，本藥品＿＿＿＿。          ← 留白
四、經詢問其他廠商，＿＿＿＿＿＿。                ← 留白
五、今{第一項藥商}等來函建議提高健保支付價(P-)：  ［表5］
六、依「全民健康保險藥物給付項目及支付標準」第35條特殊藥品支付價格訂定原則，
    本案藥品就不同核價方式試算之支付價格及財務衝擊如下：（單位：元；財務衝擊為年度推估）
    （一）{藥品名稱}（藥品代碼：{CODE}）  ［核價試算表］
         　整組每月平均申報金額({YR3}年)：{x} 元；同分組近三年平均申報量：{x}
    （二）…（每項藥品一張表）
    註1／註2／第40條說明／※兩行
七、本案經諮詢藥品專家之意見如下：
    （一）專家意見一(P-)：＿＿＿＿＿＿      ← 留白，固定 3 項
    （二）專家意見二(P-)：＿＿＿＿＿＿      ← 留白
    （三）專家意見三(P-)：＿＿＿＿＿＿      ← 留白
八、本案是否提高藥價及方式？提請全體諮詢專家共同討論。
```

> `(P-)` 一律置於冒號左方，供承辦人手填附件頁碼。
> 說明二各分項編號由 `numbering.xml` 真編號自動接續：藥品各佔一項，之後才是（表1）（表2-A）（表2-B），
> 因此上表的（三）（四）（五）會隨藥品項數位移。

### 5.3 說明一 · 來文串接（`joinLetters`）
**不分項。** 一項藥品可帶多筆文號（同 `CODE` 多列，見 §2.1）。 同一藥商的多筆來文集中，**只在該藥商第一筆前冠藥商名稱**；各筆以「、」分隔，最後一筆以「及」連接。
```
一、依據拜耳115年3月14日健保審字第1150001234號函、115年3月20日健保審字第1150005678號函及
    諾華115年3月25日健保審字第1150009999號函辦理。
```

### 5.4 說明二 · 兩個對照轉換

| 來源 | 值 → 提案用語 |
|---|---|
| `主檔.藥品分類` | 1→原開發廠藥品、3→BA/BE學名藥品、4→一般學名藥品、5→生物相似性藥品、9→BE對照品（查無 → 印 `藥品分類_名稱`） |
| `主檔.分類` | 1→第一、2→第二、3/4→第三A、5/6→第三B |

「收載超過／未滿 15 年」＝ 基準民國年 − `收載日期` 民國年 ≥ 15；無法判讀 → 印「超過／未滿」讓人工圈選。

### 5.5 表1 · 同分組（全案合併一張）

1. 取本案所有項目的**分組代碼聯集**，納入這些分組的全部項目（不用 ATC 過濾，呈現的是分組資訊）
2. 排序：**分組代碼 ↑ → `AMT{YR3}` ↓**
3. **列出規則同 §3.3.1 規則 B**：支付價 ≠ 0，或 支付價 ＝ 0 且不良品暫停支付註記 = 'Y'。
   後者的現行支付價欄以「**－**」顯示（不印 0），並於表格下方加一行備註說明有幾項。
   未列出的項目其金額**仍計入占率分母**（分母恆為整組）
4. 本案項目在**項次下方**加粗體「（本案藥品）」

| 欄位 | 來源／計算 |
|---|---|
| 項次 | 流水號；本案項目在其下方加粗體「（本案藥品）」 |
| 藥品名稱／藥商 | `藥品名稱`、`藥商名稱 → 藥商簡稱` |
| 現行健保支付價({年}年{月}月)(元) | `PRICE{年月}` |
| {YR3}年整組申報金額(元) | `AMT{YR3}`（已套 0 元歸零） |
| {YR3}年占率 | 該項目金額 ÷ **其所屬分組**的金額總計 |
| {YR3}年整組申報量 | `QTY{YR3}`（已套 0 元歸零） |

> 欄序：… 現行支付價 → **申報金額 → 占率 → 申報量**。
> 「整組每月平均申報金額(元)」欄**已刪除**，申報量移到最末欄。

> 占率分母用「同分組」而非全表合計 — 本表可能含多個分組，全表合計會失真。

### 5.6 表2 · 同 ATC7 碼（實為分組收載資訊）⭐

1. 由本案 `ATC7碼`（依 `ATC_MODE`）找出相符項目，取得其**分組代碼集合**
2. 每個分組的**全部項目都納入統計**，不論 ATC7 是否相符 — **有些分組會把不同 ATC7 碼歸為同一分組**
3. 整組支付價**皆為 0** 的分組不列出
4. **ATC7碼／ATC名稱兩欄**（置於分組名稱右方）：
   - **同ATC前5碼的表（PREFIX5）固定顯示**
   - 同ATC7碼的表（FULL）僅在真的出現「一個分組含多個 ATC7 碼」時顯示
   - 顯示這兩欄時，同一分組的多個 ATC7 碼**分列呈現各自數據**（項目數／支付價／金額／數量皆為該 ATC7 子集），
     **項次與分組名稱以垂直合併儲存格呈現**
   - 不顯示時每個分組一列，數字為整個分組的項目數
   - **兩種情況欄名一律為 `項目數`**（數字＝該列所代表的範圍：整組，或該組該 ATC7 碼底下）
5. 本案所屬分組在**項次下方**加粗體「（本案藥品）」
6. 金額／數量欄名帶年度：`{YR3}年整組申報金額(元)`、`{YR3}年整組申報量`；支付價欄帶年月

**欄名**：`分類分組品項數`（舊）→ `同分組項目數`（第16次）→ **`項目數`**（第17次，兩種情況統一）。
計數規則（同 §3.3）：`PRICE ≠ 0`，或 `PRICE = 0` 且 `不良品暫停支付註記 = 'Y'`。

**現行健保支付價欄不顯示 0 元**：相異價格串接時略過 0；價格 0 且不良品註記 Y 者**項目數仍計入**，改於表格下方以小字備註：

```
註：{分組名稱}之項目數包含 {x} 項暫時停止支付項目。
```
每個有此情形的分組各一行，緊接在該表下方（14pt→11pt、縮排 640 twips）。

**`ATC_MODE = BOTH`**：表2 拆成**兩張獨立的表**，各自編號、各自備註 —
「同ATC7碼」（7 碼全等）與「同ATC前5碼」（前 5 碼相同）。
多列藥品混用不同 `ATC_MODE` 時，凡出現 `FULL`／`BOTH` 者進第一張表，凡出現 `PREFIX5`／`BOTH` 者進第二張表；
只有其中一種時只出一張。Word 說明二的（三）（四）由真編號自動接續。

### 5.7 表5 · 廠商建議（每項藥品一列）
`藥品名稱`｜`分類分組名稱`｜`現行支付價({年}年{月}月)(元)`｜`廠商建議價(元)`｜
`{YR3}年廠商占率`｜`廠商成本(元)`｜**`十國藥價中位數(元)`**（原「十國中位價」）
廠商占率 ＝ 本案項目 `AMT{YR3}` ÷ 同分組總計。十國藥價中位數空或 0 → 印「無」。

### 5.8 核價試算表（合併儲存格，每項藥品一張）

```
┌────────┬──────────┬──────────────────────────────────┐
│ 健保價 │廠商建議價│            核價方式               │
├────────┼──────────┼─────────────────┬────────────────┤
│  41    │   48.4   │十國藥價中位數(註1)│參考成本價(註2) │
├────────┴──────────┼────┬────┬───┬───┬───┬───┤
│     加計比例       │ 0% │10% │15%│20%│25%│30%│
├───────────────────┼────┼────┼───┼───┼───┼───┤
│   調高後支付價     │ …                              │
│     整體藥費       │ …                              │
│     財務衝擊       │ …                              │
└───────────────────┴────────────────────────────────┘
```
- 檔數由 `十國加計比例`／`成本加計比例` 決定，各案可不同
- 十國中位價缺漏 → 十國欄整欄印「無」，只呈現成本法
- 表頭「十國藥價中位數<sup>註1</sup>」「參考成本價<sup>註2</sup>」的**註記以上標呈現**
- 每張核價表**下方一行備註**：`整組每月平均申報金額({YR3}年)：x 元；同分組近三年平均申報量：x`
- 註1／註2 依級距動態帶，前綴「參考十國藥價法」「參考成本價法」；條文相同的藥品合併只印一次，領證說明另外分列
- 第 40 條說明只印本案矩陣**實際出現**的級距句

---

## 6. Excel 四分頁

### 6.1 篩選（對應原 SAS 巨集變數，僅影響 Excel）
`ATC7碼`／`分組代碼`／`分組名稱`／`CODE`／`藥品名稱` 五欄，前綴比對、多值以空白或逗號分隔、任一命中即納入。

### 6.2 各分頁

| 分頁 | 內容 |
|---|---|
| 1. 藥價核定試算 | 逐項藥品的核價矩陣＋註1／註2＋分組每月申報金額、近三年平均申報量 |
| 2. 總覽篩選_分組統計 | 依「分組代碼 × ATC 資訊」分群（欄名 `項目數`）；同分組跨多 ATC 時結尾加一列「分組小計」；整組項目數為 0 的分組排最後 |
| 3. 總覽篩選_項目明細 | 排除支付價 0 元 |
| 4. 總覽篩選_項目明細(含0元) | 全部項目，另加 `生效日期`（民國 YYY-MM-DD）、`不良品暫停支付註記`、`不良品暫停支付前價格` |

**明細排序**
```
調劑大類 ↑ → 分組代碼 ↑ → 支付價 0 元者置後
  → Sort_Weight ↓   // 依「分組代碼＋CODE 前 8 碼」加總當期 AMT（許可證層級）
    → 當期 AMT ↓ → CODE ↑
```
**明細占率分母**＝**整個分組**當期總計（含 0 元項目），故分頁 3 的占率不會加總為 100%。

### 6.3 格式（色系參考 IBM Carbon 淺色調）

| 項目 | 設定 |
|---|---|
| 字型 | Microsoft JhengHei 10pt；標題 14pt 藍 `#0F62FE` |
| 表頭 | 底色 `#D0E2FF`、粗體、置中、換行、列高 34 |
| 框線 | 全表細框線 `#E0E0E0` |
| 小計列 | 底色 `#F4F4F4` 粗體 |
| 本案藥品 | 底色 `#EDF5FF` 粗體 |
| 數字格式 | 金額／數量 `#,##0`；支付價 `#,##0.###`（自訂 numFmt 164） |
| 凍結 | 分頁 2/3/4 凍結第 1 列 |
| 自動篩選 | 分頁 2/3/4 全表 |
| 欄寬 | 依內容自動估算（全形字算 2），下限 8、上限 42 |
| 合併儲存格 | 分頁 1 的「核價方式／十國藥價中位數／參考成本價」跨欄 |

---

---

## 6.5 提案附件 Excel

**用途**：Word 說明二（二）（三）（四）所留 `(P-)` 頁碼欄位所指的紙本附件。版面依「專家提案附件.pdf」。

**分頁：由介面步驟 04 的核取方塊決定，與 `ATC_MODE` 無關**

| 分頁 | 納入的分組 | 預設 |
|---|---|---|
| `1.同分組` | 本案所有項目的**分組代碼聯集** | **固定產出**，不可取消 |
| `ATC前1碼` | 本案 ATC7 碼**前 1 碼**相同者所屬的分組 | 不勾（範圍極大） |
| `ATC前3碼` | 前 3 碼相同者所屬的分組 | 不勾 |
| `ATC前4碼` | 前 4 碼相同者所屬的分組 | 勾 |
| `ATC前5碼` | 前 5 碼相同者所屬的分組 | 勾 |
| `ATC7碼` | ATC7 碼**全等**者所屬的分組 | 勾 |

> 分頁編號依實際勾選結果連號（例：只勾前5碼與ATC7碼 → `1.同分組`／`2.ATC前5碼`／`3.ATC7碼`）。
> 介面元件：`atcL1`／`atcL3`／`atcL4`／`atcL5`／`atcL7`；`selectedAtcLevels()` 回傳碼數陣列，
> 傳給 `buildAttachmentWorkbook(model, rep, priceYear, levels)`。`levels` 省略時全部產出。

> ⚠️ **ATC 分頁是「項目層」比對，不做分組擴張**（與 Word 表二不同）
>
> | | 比對方式 | 為什麼 |
> |---|---|---|
> | Word 表二 | ATC 挑出分組 → 該分組**全部項目**納入 | 表二每一列是一個**分組**，呈現的是分組層資訊 |
> | 附件 ATC 分頁 | **只納入 ATC 本身相符的項目** | 分頁標題就是「相同 ATC n 碼藥品清單」，是項目層清單；若比照表二擴張，會混入 ATC 完全不同的藥 |
> | 附件 `1.同分組` | 本案項目所屬分組的**全部項目** | 這張才是分組層，故與 Word 表一／表二對得上 |
>
> `len = 7` 採**全等**（同 `ATC_MODE=FULL`）；`len < 7` 採前綴比對（同 `PREFIX5`）。
> `ATC7碼` 留白的項目不會出現在任何 ATC 分頁。
> ATC 分頁的項目數為巢狀包含：前1碼 ⊇ 前3碼 ⊇ 前4碼 ⊇ 前5碼 ⊇ ATC7碼。
> Word 說明二的 `(P-)` 只用到「同分組」「ATC7碼」「ATC前5碼」三張，其餘三張供承辦人備查。

> **量體實測**（模擬主檔 46,727 筆 / 15,576 分組，本案藥品 ATC 為 `A` 開頭）：
> 整備 0.3 秒、附件產出 0.8 秒、檔案 1.5 MB — 效能無虞。
> `ATC前1碼` 的項數就是該英文字母在主檔的總數（A=6,624、N=5,505、D=5,416、J=5,144、B=5,040、R=4,628…），
> 跑哪一案都是這個量級，**這張是電子查閱用的，不是拿來印的**。
> 鎖定列高後 A4 橫向列印頁數：前1碼 188 頁、前3碼 63 頁、前4碼 73 頁、前5碼 19 頁、ATC7碼 2 頁、同分組 1 頁。

**列出範圍**：§3.3.1 規則 B（與 Word 表一、表二 `項目數`、四分頁分頁 3 完全相同）

```
支付價 ≠ 0                              → 列出
支付價 = 0 且 不良品暫停支付註記 = 'Y'    → 列出（支付價欄顯示「－」）
支付價 = 0 且 無註記                      → 不列出
支付價欄位空白（null）                    → 不列出
```

**分組小計 ＝ 本分頁納入範圍內、該分組全部項目的加總**（採規則 A 年度歸零後，**含依規則 B 未列出的 0 元項目**）：

| 分頁 | 納入範圍 | 小計等於 |
|---|---|---|
| `1.同分組` | 整組全部項目 | **與 Word 表二「整組申報金額／申報量」、四分頁分頁 2「分組總計」同一個數字** |
| ATC 各分頁 | 該 ATC 相符的項目 | 該分組在本頁的部分（範圍不同，本就不該等於整組） |

**不印小計的兩種情況**

1. **該分組一列都沒印出**（整組支付價皆為 0 且無不良品註記）→ 整個分組不出現，連小計都沒有
2. **分組代碼留白** → 那是一群各自獨立、未編分組的項目，不是一個分組；照常列出，但**不印分組小計**，
   且排序上一律置於最後

**沒有總計列。** 各分頁只有分組小計。

**小計列整列灰底**：`sheetXml` 會為「空白但有指定樣式」的儲存格寫出 `<c r s/>`，
否則空欄位不會被寫進 XML，底色與框線會在中間斷掉。此修正同時讓所有分頁的表格框線連續。

**欄位（20 欄，A～T）**

```
A 分組代碼   B 項目   C ATC7碼   D ATC名稱 ★新增   E CODE   F 中文名稱   G 藥品名稱
H 支付價{年月}   I 藥品分類_名稱   J 必要藥品   K 分組名稱   L 劑型   M 適應症   N 藥商名稱
O {YR1}年申報量  P {YR1}年申報金額  Q {YR2}年申報量  R {YR2}年申報金額
S {YR3}年申報量  T {YR3}年申報金額
```

- 第 1 列：分頁標題（置於 B 欄，合併 B～T）
- 第 2 列：說明列（合併 B～T）
- 第 3 列：欄名

**灰底小計**：每個 `分組代碼` 結束時插入一列**分組小計**（底色 `#F4F4F4` 粗體），
最末再加一列**總計**。數字採本專案一致原則 —— 用 **0 元項目年度歸零後**的 `amtAdj`／`qtyAdj` 加總，
與表一／表二／四分頁報表同源，因此各表數字必然一致。

**排序**（同四分頁報表的分頁 3）
```
調劑大類 ↑ → 分組代碼 ↑ → 支付價 0 元者置後
  → Sort_Weight ↓   // 依「分組代碼＋CODE 前 8 碼」加總當期 AMT
    → 當期 AMT ↓ → CODE ↑
```

**列印設定**

| 項目 | 設定 |
|---|---|
| 紙張／方向 | A4（`paperSize="9"`）、**橫向** |
| 縮放 | `fitToWidth="1" fitToHeight="0"`（寬度縮成一頁，長度不限） |
| 凍結 | 前 3 列（標題／說明／欄名） |
| **列高** | `ATC前1碼`、`ATC前3碼` 兩張**鎖定固定列高 20**（適應症等長欄位只顯示第一行，不隨內容變高）；其餘四張維持自動列高 |
| 每頁重複標題 | `_xlnm.Print_Titles` = `$1:$3` |
| **列印範圍** | `_xlnm.Print_Area` = `$B$1:$T${最末列}` — **A 欄「分組代碼」不列印** |

> **為何分組代碼放 A 欄**：需求是「印出時隱藏分組代碼，但同仁審查時仍要看得到」。
> Excel 的隱藏欄在螢幕上也看不到，因此改用「放在最左欄 + 列印範圍從 B 欄起算」——
> 螢幕完整可見、可篩選可複製，列印時自動落在範圍外。

**本案藥品**：`藥品名稱` 欄以淡藍底 `#EDF5FF` 粗體標記。

### 6.6 主檔 `分組代碼` 留白的處理 ⭐

實際主檔中，院內調製品項（TPN、醫院用藥等）的 `分組代碼` 可能是**空白**，而且筆數很多。
`buildGroupModel` 依 `分組代碼` 彙總，空白值會被歸成**同一個「空白分組」**。
若讓它參與「ATC → 分組 → 整組項目」的擴張，一筆 ATC 相符的空白分組項目，
就會把主檔中**所有**空白分組的品項全部拉進來。

**防護（三處）**

| 位置 | 作法 |
|---|---|
| Word 表一（同分組） | 空白分組代碼**不作為擴張依據**；本案藥品本身若分組代碼留白，只列它自己 |
| Word 表二（同 ATC 碼） | 空白分組代碼不擴張；且**不列出**未編分組的項目 —— 表二是分組層的表，未編分組者不成一個分組。這些項目仍會依 ATC 出現在提案附件的 ATC 分頁（項目層清單） |
| 提案附件 | ATC 分頁本就只做項目層比對，不受影響；`1.同分組` 分頁同表一的處理；未編分組者**不印分組小計**、排序置最後 |
| 四分頁 · 分頁2 | 分組代碼欄顯示 `(未填分組代碼)`，且**不套用分組層彙總**（支付價區間、近三年平均申報量留空） |
| 核價（`buildItemCalc`）／表五占率 | 分組代碼留白的本案藥品**不使用空白分組的彙總**，級距與平均申報量印「－」 |

**另外**：`buildItemCalc` 與表五的廠商占率，對分組代碼留白的本案藥品**不使用「空白分組」的彙總**
（否則級距、近三年平均申報量、財務衝擊會用到一堆不相干品項的總計）——
`grp` 視為空物件，級距與平均申報量印「－」。

**介面警示**：載入主檔後，若偵測到 `分組代碼`／`ATC7碼` 留白、或支付價 0 但 `生效日期` 留白，
會在紀錄區各出一行黃色警告並附筆數。

## 7. 技術架構

### 7.1 零外部依賴
| 需求 | 作法 |
|---|---|
| 讀 `.xlsx` | 手寫 ZIP 中央目錄解析 ＋ 瀏覽器內建 `DecompressionStream('deflate-raw')` ＋ 正規表示式掃工作表 XML（避開 DOMParser 在大主檔的記憶體問題） |
| 寫 `.docx` / `.xlsx` | 手寫 CRC32 ＋ ZIP 打包器（有 `CompressionStream` 就 deflate，否則 stored）＋ 手組 OOXML |
| 讀 `.csv` | 自寫解析器（含 BOM、引號跳脫） |

不依賴任何外部函式庫。第 21 次改版拆成多個 `.js` 後，`index.html` 以 `<script src>` 載入同目錄下的 `assets/js/**`（classic script，`file://` 直接開檔也能載入）。除 Google Fonts 外仍不連外網；字型連不到時自動降階為系統字體，功能不受影響。

### 7.2 原始碼結構

第 21 次改版把單檔 `case_template.html` 拆成 13 個模組。舊的 `build/` 分段合併流程**已廢止**，
不要再用；`legacy/case_template.html` 是重構前的凍結副本，只供比對，不再維護。

模組之間以**全域函式**互相呼叫（無打包步驟、無 ES module、無 bundler），
`<script>` 的載入順序即相依順序。每個檔案開頭都是 `'use strict';`。

| 模組 | 內容 | 路徑 A | 路徑 B |
|---|---|:--:|:--:|
| `format-utils.js` | `esc`／`num`／`fmt`／`money`／`txt`／`ad8`／`toRocDate`／`fpClean`／`floorAt`／**`truncatePrice`**／**`truncate2`**／**`chineseAmount2`**／`chineseAmount`／`formatAmt`／`adaptivePct`／`drugClassLabel`／**`essentialLabel`**／`catLabel`／`catOrdinal`／`pyLabel` | ✅ | ✅ |
| `parser.js` | ZIP 讀寫、XLSX／CSV 解析 | ✅ | ✅ |
| `xlsx-writer.js` | `stylesXml`／`sheetXml`／`autoCols`／`buildXlsxBook`／`buildXlsx`／`styleForHeader`／`headRow`／`dataRow` | ✅ | ✅ |
| `group-model.js` | `buildGroupModel`／`excludeMaster`／`resolveYearWindow`／**`parseDataRange`**／`countValid`／`isCounted`／`priceRange`／**`suspFlag`**／**`ingredientKey`** | ✅ | ✅ |
| `app.js` | `DUAL_TRACK` 狀態、路徑分流、模組延遲載入、UI 事件 | ✅ | ✅ |
| `exporters/sas-workbook.js` | `buildSasWorkbook`（分頁 1 以 `skipPriceCalc` 控制）／`parseFilter`／`passFilter` | ✅ | ✅ |
| `exporters/query-workbook.js` | `buildQueryWorkbook`（路徑 A 的 3 分頁查詢表） | ✅ | ❌ |
| `price-calc.js` | `article40RoundDown`／`a10RefPrice`／`costRefPrice`／`amtTier`／`buildPriceCalc`／`parsePcts`／加成級距常數 | ❌ | ✅ |
| `case-report.js` | `buildCaseReport`／`buildItemCalc`／`mergeCaseRowsByCode`／`joinLetters`／`selfMark`／`T1H`／`T5H`／`calcTableRows` | ❌ | ✅ |
| `docx-writer.js` | DOCX OOXML 引擎（`para`／`segRuns`／`tcell`／`docTable`／`docTableRaw`／`numberingXml`／`buildDocx`） | ❌ | ✅ |
| `exporters/docx-report.js` | `reportToDocx`／`t2RawRows` | ❌ | ✅ |
| `exporters/attachment-workbook.js` | `buildAttachmentWorkbook`／`attachSheet`／`atcRowsByLen` | ❌ | ✅ |
| `template.js` | 案件需求檔範本 `TPL`／`tplHelpSheet` | ❌ | ✅ |

**載入策略**：`index.html` 靜態載入前 7 個（路徑 A 需要的全部）；標記 ❌ 的 6 個模組
在使用者**選擇路徑 B 時才由 `loadPathBModules()` → `injectModule()` 載入**（依序，`async=false`）。
因此路徑 A 的頁面上不存在 `price-calc.js`／`case-report.js`／`docx-writer.js`／`docx-report.js`，
無法誤觸試算邏輯 —— 這是 INTENT「架構約束」的實作方式。

**單檔版**（`node build/bundle.mjs` → `dist/case_template.html`）：
共用模組直接內嵌為 `<script>`；路徑 B 模組內嵌為 `<script type="text/plain" data-mod="…">`，
`injectModule()` 偵測到該區塊時改用 `script.textContent` 執行。
**執行時機與分檔版完全相同** —— 選路徑 B 之前 `typeof buildCaseReport === 'undefined'`，
`test/ui-bundle.mjs` 會實測這一點。改完 `assets/js/**` 要重跑打包器。

**測試**
```bash
# 邏輯 + 檔案產出（Node，用 vm 依序載入模組，不需 build 步驟）
node smoke.mjs
# 端對端（Playwright，真的上傳 xlsx、按按鈕、抓 console error 與下載）
node ui.mjs
# 版面檢查
soffice --headless --convert-to pdf out.docx --outdir conv && pdftoppm -png -r 78 conv/out.pdf conv/pg
```

### 7.2.1 雙軌狀態機（`DUAL_TRACK`）

```javascript
DUAL_TRACK = {
  path: null,                  // 'PATH_A' | 'PATH_B'
  priceYearMonth: null,        // '11508'
  masterData: { parsed, model, exclusions:{tpn,offnet}, priceCols },
  pathA:      { sheets:{sheet1,sheet2,sheet3} },
  pathB:      { caseData, targets, outputs:{word,xlsxMain,xlsxAttach} }
};
```

**操作流程**：上傳 fMaster →（缺 `CODE`／`分組代碼`／`ATC7碼`／`PRICE{年月}` 之一即退回）
→ 掃描 `PRICE{年月}` 欄位列成下拉 → 選年月（欄位不存在則不解鎖）→ 選路徑
→ 路徑 B 才出現 fCase 上傳區 → 產出。

**清空規則**

| 事件 | 清空 | 保留 |
|---|---|---|
| 上傳新 fMaster | `DUAL_TRACK` 全部 | 無 |
| 改選 price 年月 | `masterData.model`／`pathA`／`pathB` 產出 | 兩個檔案本身 |
| 切換 A → B | `pathA.sheets` | `masterData` |
| 切換 B → A | `pathB` 全部（**先跳確認對話框**） | `masterData` |
| 上傳新 fCase | `pathB.outputs` | `masterData`＋`pathB.caseData` |

> ⚠️ 路徑 A 沒有案件需求檔，`excludeMaster()` 的 `keepCodes` 為空；
> 路徑 B 則保留本案 CODE。這是兩條路徑母體唯一可能不同之處，切換路徑時模型會重算。

### 7.3 下載機制

本機開檔用 `<a download>`；線上 Artifact 版走 `claude.use('downloads')` 能力。
線上版拿不到能力時會顯示紅色徽章提示改用本機檔。
`.xlsx` 在線上版不被允許 → 範本自動降級為 `.csv`；**四分頁報表與提案附件無降級路徑**，線上版會直接失敗，必須用本機檔。

---

## 8. 注意事項 / 已知差異

### 8.1 與既有 SAS 程式的三處刻意差異

| # | 差異 | 說明 |
|---|---|---|
| 1 | **級距邊界改「小於等於」** | SAS 用 `low -< 500000`，恰好 500,000／1,000,000 會落下一級距；條文寫「小於等於」。已依條文改 `≤`。邊界案件結果會與舊報表不同。 |
| 2 | **級距判定所用金額** | SAS 以分組代碼取「第一筆 ATC 列」的總計；本工具一律用**整個分組代碼的總計**。分組對單一 ATC7 時兩者相同。 |
| 3 | **浮點誤差防護** | SAS 直接對機器浮點值 floor，少數金額少算一分錢（例：成本 3.8 加成 20%，正確 4.56，SAS 得 4.55）。本工具先清誤差再截斷。 |

> 若要把第 3 項同步回 SAS：把 `floor(x*100)/100` 改成 `int(round(x*100, 1))/100`。

### 8.2 待確認 / 待辦
- [ ] 用**實際藥品主檔**跑一次完整流程，特別確認 §5.6 表2「一個分組跨多個 ATC7 碼」的情境（目前只用假資料驗過）
- [ ] 核對表1／表2 的數字與現行報表
- [x] ~~提案附件 `ATC前1碼` 分頁的量體~~ → 已用 46,727 筆模擬主檔實測：產出 0.8 秒、1.5 MB，效能無虞；列印頁數過大屬預期，該張定位為電子查閱，前1碼／前3碼已鎖定列高降低頁數
- [ ] `ATC前4碼` 目前仍是自動列高，實測 73 頁 **多於已鎖列高的前3碼（63 頁）**。若覺得排序上不合理，可比照鎖定列高（一行程式：`ATT_ATC_LEVELS` 該列加 `fixedRow:true`）
- [ ] 主檔 `生效日期` 空白／格式異常的 0 元項目，會**完全不做年度歸零**，舊年度資料會整包計入小計。首次跑真實主檔時掃一次 0 元項目的生效日期。
- [ ] 主檔若缺 `中文名稱`、`必要藥品`、`劑型` 欄，提案附件對應欄位會留空（不報錯）。確認實際主檔有這些欄。
- [ ] `build/` 分段檔與 `case_template.html` **已不同步**（見 §7.2 警告），擇期切回或放棄分段架構
- [ ] PPTX 產出（ZIP 打包器可直接沿用，只需換 OOXML）
- [ ] 同仁使用說明文件

### 8.3 環境限制
- **裝置橋接常斷線**，檔案多半得從對話下載後手動覆蓋
- 線上 Artifact 版**沙箱會擋下載**（除非 `downloads` 能力可用），真正給同仁用的是本機 `case_template.html`

---

## 9. 驗證紀錄

| 項目 | 結果 |
|---|---|
| 成本法逐步對拍 | 14 組測試值，除浮點防護造成的差異外全數與 SAS 相符 |
| 十國法手算 | `218.75 × 1.2 = 262.5 → 262` ✓ |
| 成本法手算 | `130.55 × 1.5 = 195.825 → 195.82 × 1.0505 = 205.71 → 205`（領證）、`→ 195`（未領證）✓ |
| 浮點防護 | `3.8×1.2 → 4.56`（原 4.55）、`article40RoundDown(49.999999999999996) → 50`（原 49.9）；`4.55999 → 4.55` 未被誤清 ✓ |
| 級距邊界 | 500,000→20%／500,001→10%／1,000,000→10%／1,000,001→0% ✓ |
| 0 元年度歸零 | 生效民國 111 的項目，112～114 金額與數量全歸零 ✓ |
| ZIP round-trip | 自寫 writer 產出 → 自寫 reader 讀回 ✓ |
| 檔案開啟 | docx／xlsx 以 LibreOffice 開啟並轉 PDF，版面、編號連續性、合併儲存格皆正確 ✓ |
| 同 CODE 多列 | 1 藥品 × 5 筆文號（僅首列填值）→ 1 項、說明一 5 筆、表五 1 列、核價表 1 張 ✓；值填在第 2 列、文號重複、混填 `ATC_MODE` 亦正確 ✓ |
| 端對端 | Playwright：上傳兩檔 → 預覽 → 產出 docx ＋ xlsx，無 console error ✓ |
| 主題 | 淺色／深色皆通過 ✓ |
| **第 17 次改版** | |
| `ATC_MODE` 預設 | `FULL`／`PREFIX5`／`BOTH`／留空 四種輸入 → 分別得 FULL／PREFIX5／BOTH／**BOTH**，表二張數與附件分頁數皆正確 ✓ |
| 0 元年度歸零（附件小計） | 同組四個 0 元項目，生效 115／113／111／無日期 → 114 年分別為 **保留**／歸零／歸零／**保留**；分組小計 = 100+13+0+43 = 156 ✓ |
| 附件 ATC 巢狀範圍 | 前1碼 8 項 ⊇ 前3碼 7 ⊇ 前4碼 6 ⊇ 前5碼 5 ⊇ ATC7碼 5 ✓ |
| 附件列印設定 | LibreOffice 轉 PDF：A4 橫向、**A 欄分組代碼未印出**、前 3 列每頁重複 ✓ |
| 附件量體（46,727 筆模擬主檔） | 整備 0.3s／附件產出 0.8s／1.53 MB；前1碼 6,624 項、前3碼 2,208、前4碼 1,104、前5碼 276、ATC7碼 30 ✓ |
| 附件鎖定列高 | 前1碼 434→**188 頁**、前3碼 145→**63 頁**（總頁數 674→346）✓ |
| 表二合併儲存格 | 一分組跨 2 個 ATC7 碼 → 分 2 列、各自數據，項次與分組名稱垂直合併 ✓ |
| 註1／註2 上標 | 轉 PDF 後確認為上標，非括號文字 ✓ |
| （本案藥品）標記 | 位於項次下方、僅該四字加粗（8pt）、資料列其餘不加粗 ✓ |
| 編號縮排 | 一、起排 1 字元（280 twips）、（一）起排 1.5 字元（420 twips）✓ |
| 語法 | `node --check` 全檔通過；全檔無 `<script src>` ✓ |
| **第 18 次改版** | |
| 附件 ATC 分頁範圍 | 重現實際案件（本案 ATC7=B05XA01、主檔 347 筆分組代碼留白）：ATC7碼分頁由 **385 項修正為 16 項**（全部 ATC7 相符）；前5碼／前4碼 16、前3碼／前1碼 330（＝16+294+20）✓ |
| 空白分組防護 | 加入 3 筆「B05XA01 且分組代碼留白」→ ATC7碼分頁 19 項（未拖入其餘 344 筆空白分組品項）；Word 表二 5 分組 23 項目，多出的一列為「（主檔未填分組代碼）」✓ |
| **第 19 次改版** | |
| 附件小計呈現 | 全 0 元分組（3 項）完全不出現；無總計列；小計列 20 欄樣式皆為灰底（`55555555555555999999`）；未編分組 3 筆列出但無小計、排最後 ✓ |
| **第 20 次改版** | |
| 主檔排除 | 324 筆母體（294 TPN ＋ 1 不上網）→ 排除後 29 筆；表一 7→6 項；分組 `B05POT212232` 的 114 年金額 1,050→60、數量 105→6、近三年平均量 35→2；附件 ATC前3碼 324→29 項 —— **Word 與兩份 Excel 同步變動** ✓ |
| 未編分組列 | 表二不再出現「（主檔未填分組代碼）」；該 3 筆仍列於附件 ATC 分頁 ✓ |
| **Word／Excel 對帳** | 同一分組 6 個項目（含 0元Y、0元無Y、無支付價各種組合）：<br>列出的列 → Word 表一／提案附件／四分頁分頁3 **完全相同**（A、B、C 三列）✓<br>114 年金額 **1,990**、數量 **199** → Word 表二整組申報金額／附件分組小計／四分頁分頁2分組總計 **三處相同** ✓ |

---

## 10. 變更歷程（摘要）

| 會話 | 內容 |
|---|---|
| 21 | **雙軌分流架構重構**：拆成 13 個模組（`index.html` + `assets/js/**`），新增路徑 A 背景查詢（3 分頁查詢表）與 `DUAL_TRACK` 狀態機，路徑 B 模組改為選路徑後才動態載入；`buildSasWorkbook` 分頁 1 改由 `skipPriceCalc` 控制。**規則修正**：[A2] 年度歸零補上不良品暫停支付註記判斷、[B5] 支付價空值改計入項目數、[B6] 收載年晚於視窗時平均量由 `null` 改 0、[B8] 新增同成分同劑型收載年與第三 B 大類判定、[A1] 改讀 `最新申報年度資料範圍`、[C11] 價格截斷改 `toPrecision(10)` 並修正縮放後尾差、[D15] 補「2＝生物製劑」、[D17] 必要藥品代碼改輸出意義、[D19] 改用處理原則的 `chineseAmount2`／`chineseAmount`；`[hidden]` 被作者樣式蓋過導致無效的 CSS bug |
| 9 | 全面重寫，移除 CDN 依賴（舊版 `XLSX.js` 載不到就整個失效），自寫 ZIP／OOXML |
| 10 | 移植 SAS 報表規則：第40條、成本法領證加計、金額級距、近三年平均、0元歸零、占率、排序 |
| 11 | 依「表格修改說明.pdf」改版：一份需求檔＝一案、Word 格式與真編號、說明一/二改寫、核價試算表改直向小表 |
| 12 | 說明一改不分項串接、Excel 加上格式、產出資料邏輯說明頁 |
| 13 | 移除 `QTY_ALL_`，確認級距基準與成本法公式 |
| 14 | 浮點誤差防護 |
| 15 | 本文件重寫為當前規格 |
| 20 | 標題列新增**版本徽章**（`版本 2026-08-28b`）供同仁核對；新增**主檔排除條件**（`CODE` 前 3 碼為 `TPN`、`不上網註記=Y`，預設排除，於建模型前篩掉，Word／附件／四分頁一致；本案 CODE 一律保留）；移除 Word 表二的「（主檔未填分組代碼）」列 |
| 19 | 附件：整組皆 0 元的分組不再出現（含其小計）；刪除總計列；小計列改為整列灰底（空白儲存格改為寫出帶樣式的空 cell，連帶讓全表框線連續）；未編分組代碼者不印分組小計並排序置最後；四分頁分頁2 對未編分組者不套用分組層彙總 |
| 18 | **修正附件 ATC 分頁誤把整個分組拉進來**（改為項目層 ATC 比對，`len=7` 全等）；**修正主檔 `分組代碼` 留白被當成同一個分組而造成爆量**（表一／表二／附件三處防護，表二新增「（主檔未填分組代碼）」列）；分組代碼留白的本案藥品不再誤用「空白分組」彙總算級距；附件分頁改由介面勾選；附件列出規則對齊 §3.3.1 規則 B；主檔資料品質警示 |
| 17 | `ATC_MODE` 預設改 `BOTH`；新增**提案附件 Excel**（依專家提案附件 PDF 版面、加 ATC名稱欄、灰底分組小計、A4 橫向、分組代碼排除列印；固定產出 同分組／ATC前1碼／前3碼／前4碼／前5碼／ATC7碼 共 6 分頁）；Word 表格資料列不加粗、（本案藥品）改標於項次下方且僅該四字加粗、欄名一律帶年度、表一刪每月平均欄並將申報量移末、同ATC5碼表固定顯示 ATC7碼/ATC名稱並以合併儲存格呈現分組、編號縮排改 1／1.5 字元、說明二改寫並於（一）加註 ATC7碼、說明二(二)(三)(四)/說明五/說明七三子項留 `(P-)`、十國中位價改稱十國藥價中位數、註1/註2 上標、核價表下方加分組每月申報金額與近三年平均申報量 |
| 16 | 同 `CODE` 多列合併（一項藥品多筆來文）；用語全面改「項目」；表1 占率移至整組申報金額右；表2 欄名改「同分組項目數」、支付價不顯示 0 元＋暫停支付備註；`ATC_MODE` 加 `BOTH`（表2 出兩張表）＋範本下拉選單與「填表說明」分頁；範本按鈕移至步驟 01 |

---
最後更新：2026-09-02（第 21 次改版 · 雙軌分流）
