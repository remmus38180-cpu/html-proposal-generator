# 藥品主檔資料處理原則

## ＜欄位標示說明＞

文中 `{}` 代表資料庫欄位名稱（包含如 `{QTY114}`、`{AMT115}`、`{PRICE11504}` 等帶有年份或年月之動態欄位）

---

## ＜輸出欄位命名規則＞

所有輸出欄位若涉及年度申報量、申報金額或支付價格，欄位名稱須帶有對應之年或年月後綴：

- 申報量欄位：{QTY民國年}，如 `QTY114`
- 申報金額欄位：{AMT民國年}，如 `AMT114`
- 支付價格欄位：{PRICE民國年月}，如 `PRICE11504`

---

## ＜藥品主檔檔案命名規律＞

檔名格式：`YYYYMM藥品主檔(全).xlsx`

- `YYYYMM` = 西元年月，如 `202608` = 西元2026年8月
- 民國年月換算：西元年 − 1911 = 民國年，月份不變（如 `2026年8月` → `115年8月` → `11508`）
- 當月支付價欄位：`PRICE` + 民國年月，如 `PRICE11508`
- 次月支付價欄位：`PRICE` + 次月民國年月，如 `PRICE11509`

> 範例：`202608藥品主檔(全).xlsx` → 當月 `PRICE11508`，次月 `PRICE11509`

---

## ＜藥品主檔動態欄位與日期格式說明＞

以202608藥品主檔(全).xlsx為例：

### 欄位名稱隨時間動態變化之欄位

下列欄位之名稱含民國年月後綴，隨每期藥品主檔版本不同而變動，程式不可硬編碼欄位名稱，須依使用者輸入之檔名動態解析：

| 欄位類型 | 當月範例 | 次月範例 | 規律 |
|----------|----------|----------|------|
| 支付價格 | `PRICE11508` | `PRICE11509` | 當月與次月均帶民國年月後綴 |
| 不良品暫停支付註記 | `不良品暫停支付註記`（無後綴） | `不良品暫停支付註記11509` | 當月無後綴，次月帶民國年月後綴 |
| 不良品暫停支付前價格 | `不良品暫停支付前價格`（無後綴） | `不良品暫停支付前價格11509` | 當月無後綴，次月帶民國年月後綴 |
| 不實申報鎖定註記 | `不實申報鎖定註記`（無後綴） | `不實申報鎖定註記11509` | 當月無後綴，次月帶民國年月後綴 |
| 生效日期 | `生效日期`（無後綴） | `生效日期11509` | 當月無後綴，次月帶民國年月後綴 |

### 日期欄位格式

| 欄位 | 格式 | 範例 |
|------|------|------|
| 生效日期 | `YYYYMMDD`（西元） | `20180101` |
| 收載日期 | `YYYYMMDD`（西元） | `19950301` |
| 核定日期 | `YYYY/MM/DD`（西元） | `2022/10/20` |
| 生效日期11509 | `YYYYMMDD`（西元） | `20180101` |

> ⚠️ `核定日期` 格式與其他日期欄位不同（含斜線），程式解析時須分開處理。

---

## A．年度設定與資料前處理

### [A1] 當年度基準點之設定

前端UI要求使用者輸入 {price民國年月}（如 `11508`），以其民國年設定為當年度的基準。系統須驗證輸入之民國年月能對應匯入檔案中實際存在的 {PRICE民國年月} 欄位（如 `PRICE11508`）；若對應欄位不存在，則提示錯誤並要求重新輸入。

- 有完整12個月申報資料的最近一年 = 判斷 `最新申報年度資料範圍` 之值：若涵蓋完整12個月（如 `11501-11512`），則採該民國年；若不足12個月（如 `11501-11506`），則往前推算1年（必然為完整12個月）。
- 近三年 = 包含有完整12個月申報資料的最近一年，及其前2年。

> \#範例說明：202608藥品主檔.xlsx之 `最新申報年度資料範圍` 記載為 `11501-11506`，115年資料僅6個月不完整，故以114年欄位 {QTY114} 及 {AMT114} 做計算，近三年即為112、113及114年。

---

### [A2] {QTY民國年} {AMT民國年}之採計原則

採計規則依 {不良品暫停支付註記} 與 {price民國年月} 之值分四種情形：

1. {不良品暫停支付註記} **等於 "Y"**：{QTY民國年} 及 {AMT民國年} 直接採計，無年份邊界限制。（無論 {price民國年月} 為何值）

2. {不良品暫停支付註記} **不等於 "Y"** 且 {price民國年月} **不等於 0**：直接採計，無年份邊界限制。

3. {不良品暫停支付註記} **不等於 "Y"** 且 {price民國年月} **等於 0**：{QTY民國年} 及 {AMT民國年} 之年份，小於或等於 {生效日期} 之年度者可採計；大於者設為NULL。

4. {不良品暫停支付註記} **不等於 "Y"** 且 {price民國年月} **為空值**：直接採計，無年份邊界限制。（適用於藥品費用內含於其他項目中，不另予核價但有提供代碼申報藥品銷售量或使用量之情形，此類項目雖無價格仍有申報量，不可誤判為無效資料而排除）

- \*可採計之操作型定義：情形1、2、4直接採計；情形3之 {QTY民國年} 及 {AMT民國年} 年份，小於或等於 {生效日期} 之年度者可採計。
- \*不可採計之操作型定義（僅情形3）：{QTY民國年} 及 {AMT民國年} 之年份，大於 {生效日期} 之年度，並將該等年份數值設定NULL。

> \#範例：某藥品項目之 {PRICE11504} 等於 0 且 {不良品暫停支付註記} 不等於 "Y"，其 {生效日期} 為 20120101（即民國101年）。則 {QTY101} 及 {AMT101} 可採計（年份101 ≤ 101）；{QTY102} 及 {AMT102} 起之各年度數值設為NULL（年份102 > 101）。

---

### [A3] 預設排除項目

{CODE}前3碼="TPN" 或 {不上網註}="Y"

---

## B．同分組彙總

彙總前資料須先經過[A2]及[A3]方式處理，再以 {分組代碼} 為基礎，計算同分組之年度申報數量總計、年度申報金額總計、支付價區間、項目數、近三年平均申報量及每月申報金額。

---

### [B5] 同分組項目數

符合以下任一情形，方可計為給付中之同分組項目數（即不包含已取消支付價項目數）：

1. {不良品暫停支付註記} **等於 "Y"**：計入項目數。（無論 {PRICE民國年月} 為何值）
2. {不良品暫停支付註記} **不等於 "Y"** 且 {PRICE民國年月} **不等於 0**：計入項目數。
3. {不良品暫停支付註記} **不等於 "Y"** 且 {PRICE民國年月} **為空值**：計入項目數。（適用於藥品費用內含於其他項目中，不另予核價但有提供代碼申報藥品銷售量或使用量之情形）

---

### [B6] 同分組近三年平均申報量

**1. 先彙算年度總量：**

針對近三年之各個年度（近第1年、近第2年、近第3年），先分別將該分組內所有可採計藥品項目之該年度 {QTY民國年} 進行加總，產出「近第1年總申報量」、「近第2年總申報量」及「近第3年總申報量」。

> （註：若某年度分組內所有藥品項目之 {QTY民國年} 均為NULL或均不可採計，則該年度總申報量不採計，但分母不因此縮減。）

**2. 時程判定與分母折算（後計算平均）：**

依該分組第一個收載項目之收載年份判定除數（分母）：

- 情形A（收載年在近第3年之前）：將近三年有採計之年度總申報量相加後，除以3。
- 情形B（收載年落於近三年內）：
  - 若收載年為近第3年：除以3。
  - 若收載年為近第2年：除以2。
  - 若收載年為近第1年：除以1。

分組收載年 = 同 {分組代碼}（完整12碼）之 {收載日期} 之最小值之年度；分母由收載年決定，固定為3、2或1，不隨NULL年度縮減。

> ⚠️ 注意：若分組收載年晚於近第1年，該分組近三年均無申報資料，近三年平均申報量為0，仍須列出。

**資料處理步驟建議：**

- **SAS：** 以Hash Object處理，key為完整 {分組代碼}（12碼），value為 MIN({收載日期}) 年度及各年度 {QTY民國年} 加總。同一DATA STEP內建立此hash，掃描主檔一次後直接lookup回每筆資料，不需產出中間資料集。
- **JavaScript：** 以Map處理，key為完整 {分組代碼}（12碼），掃描主陣列建立GROUP BY結果後，直接對應回每筆資料，不需獨立中間表。

---

### [B7] 同分組每月申報金額

同分組每月申報金額 = 有完整12個月申報資料的最近一年之同分組 {AMT民國年} 總計 ÷ 12

（加成級距判定之唯一依據）

---

### [B8] 同成分同劑型同成分同劑型收載年判定

依112年3月23日修正之**全民健康保險藥品價格調整作業辦法第21條**，同成分、同劑型項目中，第一個列入藥物支付標準項目之收載年，距藥商銷售資料採計期間截止年超過十五年者，屬**第三B大類**調整項目。

**基準年判定（藥商銷售資料採計期間截止年）：**

依 {price民國年月} 之月份判定基準年：
- 月份 ≥ 4月：基準年 = {price民國年月} 之民國年
- 月份 < 4月：基準年 = {price民國年月} 之民國年 − 1年

**同成分同劑型收載年判定：**

以分組代碼前8碼（ATC前3碼＋主成分英文前3碼＋主成分流水號＋活性成分數）及第10、11碼（核價劑型碼）查找同成分同劑型之所有項目，取第一個收載項目之 {收載日期} 年度為同成分同劑型收載年。

（第9碼為成分組成序號／含量流水號，第12碼為規格量流水號，兩者與含量規格有關，故排除，以涵蓋同成分同劑型不同含量規格之所有項目）

**超過15年判斷：**

基準年 − 同成分同劑型收載年 ≥ 15 → 超過15年 → 屬第三B大類

**資料處理步驟建議：**

- **SAS：** 以Hash Object處理，key為分組代碼前8碼＋第10、11碼（共10碼），value為 MIN({收載日期}) 年度。與[B6]的hash各自獨立，同一DATA STEP內並行建立，掃描主檔一次後各自lookup回每筆資料，不需產出中間資料集。
- **JavaScript：** 以Map處理，key為分組代碼前8碼＋第10、11碼（共10碼），與[B6]的Map各自獨立建立後，分別對應回每筆資料，不需獨立中間表。

> ⚠️ [B6]與[B8]為兩套不同粒度的GROUP BY，必須各自獨立計算，不可合併。

---

## C．核價公式與加成級距（支付標準第35、40條）

---

### [C9] 加成級距與加計規則

**依同分組每月申報金額（[B7]）判定：**

| 條件 | 參考該項目或國外類似品之十國藥價（中位數加算上限） | 參考廠商成本價（管銷費用加計比例上限） |
|------|----------------------------------------------|-------------------------------|
| [B7] ≤ 50萬元 | 十國藥價中位數 × 120%（+20%） | 廠商成本 × 150%（+50%） |
| 50萬元 < [B7] ≤ 100萬元 | 十國藥價中位數 × 110%（+10%） | 廠商成本 × 140%（+40%） |
| [B7] > 100萬元 | 十國藥價中位數（+0%） | 廠商成本 × 130%（+30%） |

**十國藥價中位數加算運算公式（使用者匯入中位數）：**

```
上限價 = 十國藥價中位數 × (1 + 加成率)
```

加成率依上表級距決定（20% / 10% / 0%），結果依[C12]截斷。

**參考成本價運算流程：**

1. 廠商成本（使用者匯入）× (1 + 管銷費用比例) → 原始結果
2. 消除浮點誤差（同[C11]：SAS用 `ROUND(x, 1e-10)`，JavaScript用 `toPrecision(10)`）
3. 取小數點後兩位，第三位（含）以後無條件捨去
4. 若 {領有藥物許可證} = "Y"：× 1.0505（= 1 + 5% + 0.05%）
5. 依[C12]第40條進位規則截斷

---

### [C10] 領證加計（僅適用於參考成本價）

領有藥物許可證（Y）者：核算價 × 1.0505（= 1 + 5% + 0.05%）；N者不加計。

加計拆解：
- 營業稅：5%
- 藥害救濟徵收金：0.05%
- 合計：1 + 5% + 0.05% = 1.0505

---

### [C11] 浮點誤差防護

截斷前先消除二進位浮點誤差，再依[C12]規則截斷。

**SAS：**

```sas
/* 消除浮點誤差後截斷 */
data out;
  set in;
  array prices(*) price_col1 price_col2; /* 所有需截斷的價格欄位 */
  do i = 1 to dim(prices);
    _p = round(prices(i), 1e-10); /* 先消除尾差 */
    if      _p < 5  then prices(i) = floor(_p * 100) / 100;
    else if _p < 50 then prices(i) = floor(_p * 10)  / 10;
    else                 prices(i) = floor(_p);
  end;
  drop i _p;
run;
```

**JavaScript（運算過程中產生之小數）：**

```javascript
function truncatePrice(p) {
  const v = parseFloat(p.toPrecision(10)); // 消除二進位尾差
  if (v < 5)  return Math.floor(v * 100) / 100;
  if (v < 50) return Math.floor(v * 10)  / 10;
  return Math.floor(v);
}
```

**中間截斷（成本法步驟2，固定取小數後兩位）：**

```sas
/* SAS */
_p = round(cost_with_overhead, 1e-10);
interim = floor(_p * 100) / 100;
```

```javascript
// JavaScript
function truncate2(p) {
  return Math.floor(parseFloat(p.toPrecision(10)) * 100) / 100;
}
```

> ⚠️ 藥品主檔.xlsx匯入HTML時，PRICE欄位僅供顯示，不參與核價運算，無需處理浮點誤差。

---

### [C12] 第40條進位規則

核算價小數點處理（一律無條件捨去）：

| 條件 | 處理 |
|------|------|
| 核算價 < 5元 | 取小數點後兩位，第三位（含）以後捨去 |
| 5元 ≤ 核算價 < 50元 | 取小數點後一位，第二位（含）以後捨去 |
| 核算價 ≥ 50元 | 取至整數，小數點以後捨去 |

---

### [C13] 整體藥費與財務衝擊

- 整體藥費 ＝ 調高後支付價 × 近三年平均申報量
- 財務衝擊 ＝ (調高後支付價 − 現行健保價) × 近三年平均申報量

---

### [C14] 加計比例預設檔位

- 十國加計比例：0 / 5 / 10 / 15 / 20%
- 成本加計比例：0 / 5 / 10 / 15 / 20 / 25 / 30 / 35 / 40 / 45 / 50%


## D．分類代碼與顯示格式

| 編號 | 內容 |
|------|------|
| [D18] | ATC_MODE三模式：FULL(7碼全等)／PREFIX5(前5碼)／BOTH(兩者都做，預設) |
| [D19] | 金額顯示級距（詳見下方規則） |

### [D19] 金額顯示級距規則

| 範圍 | 顯示方式 | 進位規則 |
|------|---------|---------|
| x < 1萬 | 實際數字，加千分位逗號（如 823元、2,300元） | 整數，無小數 |
| 1萬 ≤ x < 1,000萬 | 萬（如 1.2萬、20.4萬、453.2萬） | 小數1位，第2位四捨五入 |
| 1,000萬 ≤ x < 1億 | 億（如 0.34億） | 小數2位，第3位四捨五入 |
| 1億 ≤ x < 1,000億 | 億（如 2.4億） | 小數1位，第2位四捨五入 |
| 1,000億 ≤ x < 1兆 | 兆（如 0.34兆） | 小數2位，第3位四捨五入 |
| x ≥ 1兆 | 兆（如 2.4兆） | 小數1位，第2位四捨五入 |

**SAS實作（PROC FCMP）：**

```sas
function chinese_amount2(val) $40;
    length outv $40;
    if missing(val) then return('');
    v = round(val, 1);
    if v = 0 then return('0元');
    neg = (v < 0);
    av  = round(abs(v), 1);

    wan  = 10000;
    qwan = 10000000;
    yi   = 100000000;
    qyi  = 100000000000;
    zhao = 1000000000000;

    if av < wan then do;
        outv = strip(put(av, comma20.)) || '元';
    end;
    else if av < qwan then do;
        q = floor(av / wan); r = mod(av, wan);
        f = round(r * 10 / wan);
        if f = 10 then do; q = q + 1; f = 0; end;
        outv = strip(put(q, comma20.)) || '.' || strip(put(f,1.)) || '萬';
    end;
    else if av < yi then do;
        q = floor(av / yi); r = mod(av, yi);
        f = round(r * 100 / yi);
        if f = 100 then do; q = q + 1; f = 0; end;
        outv = strip(put(q, comma20.)) || '.' || put(f, z2.) || '億';
    end;
    else if av < qyi then do;
        q = floor(av / yi); r = mod(av, yi);
        f = round(r * 10 / yi);
        if f = 10 then do; q = q + 1; f = 0; end;
        outv = strip(put(q, comma20.)) || '.' || strip(put(f,1.)) || '億';
    end;
    else if av < zhao then do;
        q = floor(av / zhao); r = mod(av, zhao);
        f = round(r * 100 / zhao);
        if f = 100 then do; q = q + 1; f = 0; end;
        outv = strip(put(q, comma20.)) || '.' || put(f, z2.) || '兆';
    end;
    else do;
        q = floor(av / zhao); r = mod(av, zhao);
        f = round(r * 10 / zhao);
        if f = 10 then do; q = q + 1; f = 0; end;
        outv = strip(put(q, comma20.)) || '.' || strip(put(f,1.)) || '兆';
    end;

    if neg then outv = '-' || strip(outv);
    return(outv);
endfunc;
```

**JavaScript實作：**

```javascript
function chineseAmount2(val) {
  if (val === null || val === undefined || val === '' || Number.isNaN(Number(val))) return '';

  const raw = Number(val);
  if (!Number.isFinite(raw)) return '數值超出範圍';
  const n = Math.round(raw);
  if (!Number.isSafeInteger(n)) {
    console.warn(`chineseAmount2: 數值 ${val} 超過安全整數範圍 (±${Number.MAX_SAFE_INTEGER})，結果可能不精確`);
  }
  if (n === 0) return '0元';

  const neg = n < 0;
  const av = BigInt(Math.abs(n));

  const WAN  = 10000n, QWAN = 10000000n, YI = 100000000n,
        QYI  = 100000000000n, ZHAO = 1000000000000n;

  const qf = (v, divisor, decimals) => {
    const scale = 10n ** BigInt(decimals);
    let q = v / divisor;
    const r = v % divisor;
    const scaled = r * scale;
    let f = scaled / divisor;
    const rem2 = scaled % divisor;
    if (rem2 * 2n >= divisor) f += 1n;
    if (f === scale) { q += 1n; f = 0n; }
    return { q, f };
  };
  const fmt = (q, f, decimals, unit) =>
    q.toLocaleString('en-US') + '.' + f.toString().padStart(decimals, '0') + unit;

  let out;
  if (av < WAN) {
    out = av.toLocaleString('en-US') + '元';
  } else if (av < QWAN) {
    const { q, f } = qf(av, WAN, 1);
    out = fmt(q, f, 1, '萬');
  } else if (av < YI) {
    const { q, f } = qf(av, YI, 2);
    out = fmt(q, f, 2, '億');
  } else if (av < QYI) {
    const { q, f } = qf(av, YI, 1);
    out = fmt(q, f, 1, '億');
  } else if (av < ZHAO) {
    const { q, f } = qf(av, ZHAO, 2);
    out = fmt(q, f, 2, '兆');
  } else {
    const { q, f } = qf(av, ZHAO, 1);
    out = fmt(q, f, 1, '兆');
  }
  return neg ? '-' + out : out;
}
```

---

**[D19] 完整中文金額展開式（適用精確金額顯示）**

與上方簡化顯示（`chineseAmount2`）不同，此函數將金額完整展開為中文單位串接，適用於正式文件或需要精確金額的場合。

支援參數：
- `decimals`：小數位數，預設3，範圍0～6
- `unit`：單位字串，預設「元」

| 輸入 | chineseAmount2（簡化） | chineseAmount（展開） |
|------|----------------------|---------------------|
| 123,456,789 | 1.23億 | 1億2345萬6,789元 |
| 50,000 | 5.0萬 | 5萬元 |
| 823 | 823元 | 823元 |

**SAS實作（PROC FCMP）：**

```sas
function chinese_amount(val, decimals, unit $10) $60;
    length outv $60 decstr $20 padstr $20 fmt $10;

    /* ===== 參數防呆 ===== */
    if missing(val) then return('');

    if missing(decimals) then decimals = 3;
    decimals = floor(decimals);

    if decimals < 0 then decimals = 0;
    if decimals > 6 then decimals = 6;

    if missing(unit) then unit = '元';

    /* ===== 處理正負 ===== */
    neg = (val < 0);
    absv = abs(val);

    /* ===== 小數處理 ===== */
    intv = floor(absv);

    if decimals = 0 then do;
        intv = round(absv, 1);
        decstr = '';
    end;
    else do;
        scale = 10 ** decimals;

        /* 將小數部分轉成整數，避免直接處理浮點小數 */
        decv = round((absv - intv) * scale, 1);

        /* 四捨五入進位 */
        if decv >= scale then do;
            intv = intv + 1;
            decv = 0;
        end;

        if decv > 0 then do;
            padstr = put(decv, z18.);
            decstr = '.' ||
                     substr(strip(padstr),
                            length(strip(padstr)) - decimals + 1);
        end;
        else decstr = '';
    end;

    /* 四捨五入後為 0 → 不產生 -0 */
    if intv = 0 and decstr = '' then
        return('0' || strip(unit));

    /* ===== 中文大單位 ===== */
    rest = intv;

    array div{3} _temporary_
        (1000000000000 100000000 10000);

    array unt{3} $4 _temporary_
        ('兆' '億' '萬');

    outv = '';

    do i = 1 to 3;
        part = floor(rest / div{i});
        rest = mod(rest, div{i});

        if part > 0 then
            outv = strip(outv)
                   || strip(put(part, comma32.))
                   || strip(unt{i});
    end;

    /* ===== 最後四位 + 小數 ===== */
    if rest > 0 or decstr ne '' or outv = '' then
        outv = strip(outv)
               || strip(put(rest, comma32.))
               || strip(decstr);

    /* ===== 單位 ===== */
    outv = strip(outv) || strip(unit);

    /* ===== 負號 ===== */
    if neg then
        outv = '-' || strip(outv);

    return(outv);
endfunc;
```

**JavaScript實作：**

```javascript
function chineseAmount(val, decimals = 3, unit = '元') {
  if (val === null || val === undefined || val === '') return '';

  const raw = Number(val);

  if (Number.isNaN(raw)) return '';
  if (!Number.isFinite(raw)) return '數值超出範圍';

  if (decimals === null || decimals === undefined || decimals === '') {
    decimals = 3;
  }

  decimals = Math.floor(Number(decimals));

  if (!Number.isFinite(decimals)) decimals = 3;
  if (decimals < 0) decimals = 0;
  if (decimals > 6) decimals = 6;

  if (unit === null || unit === undefined || unit === '') {
    unit = '元';
  }

  if (raw === 0) return '0' + unit;

  const neg = raw < 0;
  const absv = Math.abs(raw);

  let intv;
  let decstr = '';

  if (decimals === 0) {
    intv = Math.round(absv);
  } else {
    intv = Math.floor(absv);

    const scale = 10 ** decimals;

    let decvInt = Math.round(
      (absv - intv) * scale
    );

    if (decvInt >= scale) {
      intv += 1;
      decvInt = 0;
    }

    if (decvInt > 0) {
      decstr =
        '.' +
        String(decvInt).padStart(decimals, '0');
    }
  }

  /* 避免 -0 */
  if (intv === 0 && decstr === '') {
    return '0' + unit;
  }

  if (!Number.isSafeInteger(intv)) {
    console.warn(
      `chineseAmount: 數值 ${val} 的整數部分超過 JavaScript 安全整數範圍，結果可能不精確`
    );
  }

  let rest = BigInt(intv);

  const zhao = rest / 1000000000000n;
  rest %= 1000000000000n;

  const yi = rest / 100000000n;
  rest %= 100000000n;

  const wan = rest / 10000n;
  rest %= 10000n;

  const yu = rest;

  const fmt = x => x.toLocaleString('en-US');

  let out = '';

  if (zhao > 0n)
    out += fmt(zhao) + '兆';

  if (yi > 0n)
    out += fmt(yi) + '億';

  if (wan > 0n)
    out += fmt(wan) + '萬';

  if (yu > 0n || decstr !== '' || out === '')
    out += fmt(yu) + decstr;

  out += unit;

  if (neg)
    out = '-' + out;

  return out;
}
```
| [D20] | 占率位數：10%以上取整數／1%≤x<10%取1位小數／未滿1%補到第一個有效數字 |


---

### [D15] {藥品分類} 與 {藥品分類_名稱} 對照

| {藥品分類} | {藥品分類_名稱} | 法規名稱 |
|-----------|---------------|---------|
| 1 | 研發廠 | 原開發廠藥品 |
| 2 | 生物製劑 | |
| 3 | BA/BE學名藥 | |
| 4 | 一般學名藥 | |
| 5 | 生物相似性藥品 | |
| 9 | BE對照品 | |

---

### [D16] {分類} 與 {分類分組類別} 對照

| {分類} | {分類分組類別} | 顯示名稱 |
|--------|-------------|---------|
| 1 | 一 | 第一大類 |
| 2 | 二 | 第二大類 |
| 3 | 三A_1 | 第三A大類 |
| 4 | 三A_2 | 第三A大類 |
| 5 | 三B_1 | 第三B大類 |
| 6 | 三B_2 | 第三B大類 |

---

### [D17] {必要藥品} 代碼對照

| 代碼 | 意義 |
|------|------|
| 0 | 一般藥品（非特殊或不可替代特殊藥品） |
| 1 | 一般藥品（非特殊或不可替代特殊藥品） |
| 3 | 不可替代特殊藥品 |
| 4 | 特殊藥品 |

---

## ＜藥品分組代碼編碼原則＞

### 分組代碼結構

| 組別 | 位置 | 長度 | 內容說明 |
|------|------|------|---------|
| 第1組 | 第1～3碼 | 3碼 | 取藥品ATC代碼前3碼。例如famotidine之ATC碼為A02BA03，取前3碼為 `A02` |
| 第2組 | 第4～6碼 | 3碼 | 取主成分英文名稱前3個字元。例如famotidine取前3碼為 `FAM`；複方藥品依ATC編碼原則認定主成分 |
| 第3組 | 第7碼 | 1碼 | 主成分流水號。依第1～2組排序後若重複則依序編入流水號（自2起，每次遞增2） |
| 第4組 | 第8碼 | 1碼 | 活性成分數（1～6及9）。1=單方；2以上=複方；9=7個（含）以上活性成分之複方 |
| 第5組 | 第9碼 | 1碼 | 成分組成序號（含量流水號）。成分組合相同但個別含量不同時，依序編入流水號 |
| 第6組 | 第10～11碼 | 2碼 | 核價劑型碼。例如注射劑=`20`、外用點眼液劑=`40`、外用點眼軟膏劑=`41`、口腔噴霧劑=`10` |
| 第7組 | 第12碼 | 1碼 | 規格量流水號。依含量或規格量由低至高遞增，最低劑量編為 `2`，每增一規格遞增2；無法區分者內定為 `1` |

> 範例：`S01ACY212412` = ATC前3碼 `S01` ＋ 主成分 `ACY` ＋ 主成分流水號 `2` ＋ 單方 `1` ＋ 含量序號 `2` ＋ 外用點眼軟膏劑 `41` ＋ 規格量 `2`（4.5GM）

---

### 流水號編碼規則

流水號以阿拉伯數字輔以英文字母，先數字再字母，序列為：

`2、4、6、8、A、C、E、G、I、K、M、O、Q、S、U、W、Y`

**編碼邏輯：** 自2開始，每新增一個規格量遞增2，預留的奇數碼供後續插入介於兩者之間的規格量使用。

> 範例：
> - 第1種規格量100mL → 編為 `2`
> - 第2種規格量200mL（大於100mL）→ 編為 `4`（跳過`3`預留）
> - 若後來新增150mL（介於100mL與200mL之間）→ 插入編為 `3`
> - 若後來新增300mL（大於200mL）→ 編為 `6`

罕藥證品項流水號以對應一般藥證代碼遞增1；若已有其他分組則往後順延。

---

### 複方藥品主成分認定順序

1. 依ATC編碼原則認定主成分
2. 維他命（除B12外）：依維他命英文及數字排序
3. 含麻醉性藥品：以麻醉藥品為主成分
4. 其他：依中樞神經刺激劑、消炎解熱劑、其他等方式排序，併依ATC代碼排序

---

### 各劑型分組原則

> 上表劑型為概念性分類，同列劑型可能包含多種核價劑型，實際核價劑型名稱可參照多模型健保資料庫之資料表 `DWM_DOSE_CODE`（劑型對照檔），欄位 `DRUG_DOSE_NAME`（藥品劑型代碼名稱）及 `DRUG_APV_DOSE_NAME`（藥品核價劑型代碼名稱）。

> ⚠️ 此處「核價劑型」與藥品許可證所載之 {劑型} 不同。健保辦理藥價調整時，將多種 {劑型} 對應至同一 {核價劑型}，以計算同分組加權平均銷售價格。例如 {劑型} 為「乾粉注射劑」或「凍晶注射劑」，均歸屬 {核價劑型}「注射劑」。

| 核價劑型 | 單方分組依據 | 複方分組依據 |
|---------|------------|------------|
| 口服錠劑（含緩釋、軟膠囊、腸溶） | 同成分、總含量 | 同配方成分、同配方含量 |
| 口服顆粒劑 | 同成分、總含量 | 同配方成分、同配方含量 |
| 注射劑 | 同成分、總含量 | 同配方成分、同配方含量 |
| 內服液劑 | 同成分、成分含量及規格量 | 同配方成分、同配方含量之規格量 |
| 外用軟膏劑（含外用點眼軟膏劑） | 同成分、成分含量及規格量 | 同配方成分、同配方含量之規格量 |
| 外用液劑（含外用點眼液劑） | 同成分、成分含量及規格量 | 同配方成分、同配方含量之規格量 |
| 外用貼片 | 同成分、總含量 | — |
| 外用錠劑 | 依口服錠劑原則 | 依口服錠劑原則 |
| 陰道軟膏劑 | 依外用軟膏劑原則 | 依外用軟膏劑原則 |
| 陰道用錠劑膠囊劑 | 依口服錠劑原則 | 依口服錠劑原則 |
| 外用噴霧劑及口腔噴霧劑 | 同成分、成分含量及Dose數 | 同配方成分、同配方含量之Dose量 |

> ⚠️ 同成分、劑型、規格品項同時具有一般藥證及罕藥證時，二者歸為不同分組，罕藥證品項於分組名稱後加註「(罕)」。

---

## 附錄

### 附錄A：網頁字體與排版規範

只要涉及網頁字體（font-family）設定或程式碼/數據排版時，預設遵循以下規範：

**1. 中英雙等寬與系統降階（Fallback）組合**

- 英文/數字/符號優先使用：`IBM Plex Mono`
- 中文字體優先使用外連思源字體，並依據 Mac/Windows 系統進行平滑降階（Fallback）
- 預設 CSS 寫法：

```css
font-family: 'IBM Plex Mono', 'Noto Sans Mono CJK TC', 'Noto Sans TC', 'PingFang TC', 'Microsoft JhengHei', monospace;
```

**2. 數字與表格對齊優化**

請自動為數據、表格或 Code 區塊加上 `font-variant-numeric: tabular-nums;` 以確保數字與數據等寬對齊。

**3. Google Fonts 外連引入**

若需要提供完整的 HTML 範例，請務必在 `<head>` 中引入 IBM Plex Mono 與 Noto Sans TC 的 CDN 連結：

```html
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:wght@400;500&family=Noto+Sans+TC:wght@400;700&display=swap" rel="stylesheet">
```

---

### 附錄B：claude-light.css 設計規範

仿 Claude.ai 網頁版淺色模式渲染樣式，供輸出HTML文件使用。

**色彩規格：**

| 元素 | 色彩 |
|------|------|
| 背景 | `#ffffff` |
| 正文 | `#1a1a1a` |
| inline code 文字 | `#A25050` |
| inline code 底色 | `#F6F6F6` |
| inline code 框線 | `#E8E8E8` |
| 引用區塊左框線（一般） | `#cccccc` |
| 警告區塊左框線 | `#f0a500` |
| 警告區塊底色 | `#fffbf0` |
| 表格 header 底線 | `#cccccc`（2px） |
| 表格 row 底線 | `#e0e0e0`（1px） |
| 分隔線 | `#e0e0e0`（1px） |

**字體規格：**

```css
/* 正文 */
font-family: 'IBM Plex Mono', 'Noto Sans Mono CJK TC', 'Noto Sans TC', 'PingFang TC', 'Microsoft JhengHei', monospace;

/* code 區塊 */
font-family: 'IBM Plex Mono', 'Noto Sans Mono CJK TC', monospace;
font-variant-numeric: tabular-nums;
```

**尺寸與間距：**

| 元素 | 規格 |
|------|------|
| 最大寬度 | 860px |
| 左右 padding | 48px |
| 正文字級 | 15px |
| 行高 | 1.7 |
| h1 字級 | 22px |
| h2 字級 | 16px |
| h3 字級 | 15px |

**特殊說明：**

- 警告區塊需手動加 `class="warn"`：`<blockquote class="warn">⚠️ 注意事項</blockquote>`
- 列印時使用 `@media print`，padding縮減為 20px 30px
- CSS採內嵌方式（`<style>`標籤），不依賴外部檔案，確保PDF列印完整
